[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tbd-dictionary-control"
version = "0.1.0"
description = "PDF belgelerindeki İngilizce bilişim terimlerini Türkçe sözlükle karşılaştıran yerel araç"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "pdfplumber>=0.10,<1",
  "openpyxl>=3.1,<4",
]

[project.scripts]
tbd-dictionary-control = "terim_etmeni.cli:main"
turkce-terim-etmeni-v2 = "terim_etmeni_v2.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
include = ["terim_etmeni*", "terim_etmeni_v2*"]
